**Gate 1 — Code Quality**
Linter: [paste exact output, e.g. "Exit 0, zero warnings" or full error list]
TODO scan: [paste grep output or "no matches found"]
Secrets scan: [paste grep output or "no matches found"]
Function length: [confirmed no functions >50 lines / list exceptions with documented reason]
